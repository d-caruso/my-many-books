/**
 * Type definitions for the web app.
 *
 * Re-export from the shared monorepo package so web/mobile/API stay aligned.
 */

export type {
  ApiError,
  AuthUser,
  Author,
  Book,
  BookCardProps,
  Category,
  PaginatedResponse,
  ScanResult,
  SearchFilters,
  SearchResult,
  Theme,
  ThemeName,
  User,
} from '@my-many-books/shared-types';
