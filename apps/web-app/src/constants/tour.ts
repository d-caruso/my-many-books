// Keep the guided-tour overlay above the app's existing surfaces.
export const TOUR_OVERLAY_Z_INDEX = 1400;

// Give step targets a brief window to appear after route/UI changes.
export const TOUR_SELECTOR_TIMEOUT = 3000;

// Let navigation settle before the next step queries the DOM.
export const TOUR_NAVIGATION_SETTLE_DELAY = 100;
